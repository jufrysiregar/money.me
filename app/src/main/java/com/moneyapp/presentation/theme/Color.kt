package com.moneyapp.presentation.theme

import androidx.compose.ui.graphics.Color

// Brand Colors
val Primary = Color(0xFF1E6091)          // Biru tua — aksi utama, AppBar
val PrimaryVariant = Color(0xFF154F78)   // Biru lebih gelap — pressed state
val Secondary = Color(0xFFD9E3F0)        // Biru muda — background kartu, chip

// Semantic Colors
val Success = Color(0xFF2D6A4F)          // Hijau — Pemasukan, status positif
val Danger = Color(0xFFF4A261)           // Oranye — Pengeluaran, warning
val DangerDark = Color(0xFFE07B3A)       // Oranye gelap — pressed/dark mode

// Background
val BackgroundLight = Color(0xFFF8F9FA)  // Abu sangat terang — background light
val BackgroundDark = Color(0xFF121212)   // Hitam Material — background dark
val SurfaceLight = Color(0xFFFFFFFF)
val SurfaceDark = Color(0xFF1E1E1E)

// Text
val OnPrimary = Color(0xFFFFFFFF)
val OnBackground = Color(0xFF1C1B1F)
val OnBackgroundDark = Color(0xFFE6E1E5)
