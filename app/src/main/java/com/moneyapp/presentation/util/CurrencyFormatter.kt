package com.moneyapp.presentation.util

/**
 * CurrencyFormatter — backward-compatible re-exports from NumberFormatter.
 *
 * All functions delegate to [NumberFormatter]. Import from here OR from
 * NumberFormatter directly; both are equivalent.
 *
 * @see NumberFormatter
 */

// Re-export all functions so existing imports continue to compile unchanged.
// (Some files import com.moneyapp.presentation.util.formatRupiah etc.)
